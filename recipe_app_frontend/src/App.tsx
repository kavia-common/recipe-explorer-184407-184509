import React from "react";
import HomeRoute from "./routes/index";

/**
 * PUBLIC_INTERFACE
 * Root application component. Currently renders the Home route directly.
 */
export default function App() {
  return <HomeRoute />;
}
